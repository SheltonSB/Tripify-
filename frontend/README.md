# Frontend

## Run locally

```bash
cd frontend
npm install
npm run dev
```

Set `VITE_API_BASE_URL` to your backend base URL (for example `http://localhost:8000`).
You can do this in `frontend/.env.local`.
