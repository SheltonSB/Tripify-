spring:
  datasource:
    url: jdbc:postgresql://db:5432/travel
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  redis:
    host: redis
    port: 6379
